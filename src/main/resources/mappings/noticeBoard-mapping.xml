<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NoticeDAO">


    <insert id="insertNotice">
        INSERT INTO NoticeBoard(cnt, category, title, writer, writerId, content, date)
        VALUES(#{cnt}, #{category}, #{title}, #{writer}, #{writerId}, #{content}, #{date})
    </insert>


    <select id="selectNotice" parameterType="com.library.notice.model.NoticeVO" resultType="com.library.notice.model.NoticeVO">
        SELECT *
        FROM NoticeBoard
        WHERE cnt = #{cnt}
    </select>


    <update id="updateNotice" parameterType="com.library.notice.model.NoticeVO">
        UPDATE NoticeBoard
        SET category = #{category},
        title = #{title},
        writer = #{writer},
        writerId = #{writerId},
        content = #{content},
        date = #{date}
        WHERE cnt = #{cnt}
    </update>


    <delete id="deleteNotice" parameterType="com.library.notice.model.NoticeVO">
        DELETE FROM NoticeBoard
        WHERE cnt = #{cnt}
    </delete>

</mapper>
