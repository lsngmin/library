/*
package com.library.service;

import com.library.domain.dto.BookRegistrationDTO;
import com.library.domain.dto.BookUpdateDTO;
import org.springframework.web.multipart.MultipartFile;

public interface BookService {
    void registerBook(BookRegistrationDTO bookDTO, MultipartFile coverImage);

    BookUpdateDTO getBookById(Long id);
    void updateBook(BookUpdateDTO bookDTO, MultipartFile coverImage);
}
 */